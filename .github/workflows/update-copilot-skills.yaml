name: 🔄 Update Copilot Skills

on:
  workflow_dispatch:
  schedule:
    - cron: "0 6 * * *"

permissions:
  contents: write
  pull-requests: write

jobs:
  update:
    uses: devantler-tech/reusable-workflows/.github/workflows/update-copilot-skills.yaml@41924797ae44fd5cc674e0914db55712da18e68f # v3.2.0
    with:
      dir: plugins
